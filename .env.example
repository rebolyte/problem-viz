# Server
PORT=3000
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=

# Deployment (local .env only, not needed on VPS)
VPS_IP=your-vps-ip
VPS_USER=root
VPS_SSH_PORT=22
# location of docker-compose.yml
VPS_COMPOSE_DIR=/var/traefik
